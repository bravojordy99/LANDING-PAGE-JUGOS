# Landing Page Jugos

Landing page de venta de jugos naturales creada con React y Vite.

## Scripts

- `npm run dev`: inicia el servidor local de desarrollo.
- `npm run build`: genera la version de produccion en `dist`.
- `npm run preview`: sirve el build de produccion localmente.
- `npm run lint`: revisa el codigo con ESLint.
